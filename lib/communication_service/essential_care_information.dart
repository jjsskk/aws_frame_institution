import 'package:flutter/material.dart';

class EssentialCareInfoPage extends StatelessWidget {
  const EssentialCareInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('필수 돌봄 정보'),
        centerTitle: true,
      ),
      body: Center(
        child: Column(
          children: [

          ],
        ),
      ),
    );;
  }
}
