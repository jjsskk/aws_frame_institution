import 'package:flutter/material.dart';

class InstitutionSummaryPage extends StatelessWidget {
  const InstitutionSummaryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('기관 요약 보고서'),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
