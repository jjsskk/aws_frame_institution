import 'package:flutter/material.dart';

class IndividualAnalysisPage extends StatelessWidget {
  const IndividualAnalysisPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('개별 분석 보고서'),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
